# 小爱课表导入器 · 乘方教务版

把你的**乘方科技教务系统**课程表，一键批量导入到小米**小爱同学**的课程表里。导入后，对你的音箱说「小爱同学，我今天第一节什么课」就能听到答案，还能设置上课提醒。

原版官方流程要在小爱里一条条手动添加课程，点十几下；本工具把教务系统的整学期课表自动抓取、解析、批量写入，**一学期只需点几下**。

> **已适配学校**：**河南师范大学**（`jwc.htu.edu.cn`）已完整实测通过——整学期课表提取、单双周合并、十个节次与官方夏/冬作息均已核对。其他使用**乘方科技教务系统**的高校大多可直接使用，字段差异见「适用学校」与「常见问题」。

---

## 一、这是什么 / 能干什么

一个开源网页工具，由三部分组成：

| 部分 | 作用 |
|---|---|
| **导入器网页** | 你看到的主页面：连接小爱 → 粘贴课表 → 预览 → 一键导入 → 管理 |
| **书签 / 油猴提取脚本** | 在你浏览器里跑，登录教务系统后一键把整学期课表抓成标准 JSON（书签方式零安装） |
| **轻量后端** | 处理小爱接口通信与课表格式转换，可本地运行或部署到 Cloudflare |

### 核心功能一览

- ✅ **乘方教务一键提取**：登录教务后点一下，自动逐周扫描全学期 → 合并单双周 → 复制到剪贴板
- ✅ **智能字段解析**：自动识别河南师大 / 标准乘方的不同字段（星期、节次、周次、教室、教师）
- ✅ **可视化预览**：导入前用星期×节次的彩色网格核对课表，确认无误再写
- ✅ **一键批量导入**：写入小爱指定课表，支持导入前清空、同步节数与时间表
- ✅ **课表管理**：查看、单条删除、全部清空、导出 JSON 备份
- ✅ **多种提取通道**：浏览器书签（推荐，零安装且自动最新）、**教务导出表格直接上传（.xls/.xlsx，零安装不碰接口）**、油猴脚本（按钮常驻）、智能粘贴（兜底），总有一种适合你
- ✅ **数据不出本机**：教务 Cookie 不进入任何第三方；小爱凭据只存在**你自己浏览器**的本地存储里（下次自动回填，免重复粘贴），除小米课表服务器外不发往任何地方

### 适用学校

使用**乘方科技教务系统**的高校。判断方法：教务网址通常是 `jwc.xxx.edu.cn` 或 `jxgl.xxx.edu.cn`，登录后地址栏带 `/new/`，课表接口叫 `getCalendarWeekDatas`。

> 已知适配并实测通过：**河南师范大学**（`jwc.htu.edu.cn`）。同系统的其他学校基本可直接用，个别字段有差异时按文末「常见问题」反馈即可。
> 如果你的学校用的是**强智教务**（jsxsd，如广州番禺 / 长沙理工 / 中南大学），请用原版工具。

---

## 二、准备工作（一次性）

### 1. 打开导入器网页

- **直接用线上地址**（最简单，推荐）：**<https://xiaoai-kebiao-zx.pages.dev>** —— 打开即用，不需要装 Node、不需要下载任何东西，手机浏览器也能用
- **本地运行**（进阶，或学校网络访问不了上面地址时用）：下载项目后双击 `启动导入器.bat`，浏览器自动打开 `http://localhost:8787`

> 书签方式必须用 `http://` 打开（本地或线上均可），不要直接双击 `index.html` 用 `file://` 打开。

### 2. 存下提取书签（推荐，零安装）

在导入器页「**2 · 提取课表**」卡片：

1. 点 **「📚 复制提取书签，粘贴到书签栏」**；
2. 按 **Ctrl+Shift+B** 显示浏览器书签栏（Chrome / Edge）；
3. 书签栏空白处右键 → **「添加网页」**，标题填「提取课表」，**网址框里 Ctrl+V 粘贴** → 保存。

> **只存一次，长期可用。** 书签本身不含提取逻辑，只是一个「去后端取脚本」的入口，因此**每次点击都自动使用最新版脚本**，不会出现脚本版本过旧的问题。
>
> 三点约束：① 后端地址在「点复制」那一刻固化进书签，之后换后端（如改用 `?api=`）要重新复制一次；② 书签依赖后端在线，本地用要保持 `启动导入器.bat` 开着；③ 书签只注入页面顶层框架，若你学校的学期码只藏在 POST 载荷、页面又没有学期下拉框，请改用下面的油猴脚本。

### 3. （可选）安装浏览器扩展 Tampermonkey

长期用、希望教务页面按钮常驻的话，可去扩展商店搜 **Tampermonkey** 安装。它是上一条的替代方案：一次安装后右下角长期挂着提取按钮，且不依赖后端随时可访问，但**脚本不会自动更新**，日后要用新功能得回网页重新覆盖粘贴一次。

### 4. 获取小爱课程表凭据

凭据是连接小爱服务器的钥匙，共三项：`appId`、`serviceToken`、`deviceId`。在**手机上**按下面五步取：

1. 打开**小爱同学 App**，进入「**课程表**」；
2. 进入课程表的**设置**页面；
3. **滑到页面最底下**，对着下方的**空白处连续点击五下**；
4. 就会进入 **Debug（开发者）页面**，点击「**获取 userinfo**」；
5. 把弹出的那段内容**整段复制**备用——回到导入器网页粘进大框，它会自动识别出上面三项。

> 这一步是全程唯一需要在手机上完成的环节。连点五下进的是小爱课程表内置的开发者入口，不同 App 版本界面细节略有差异，但位置都在**课程表设置页拉到底部的空白处**。拿到的内容里只要含上述三项就能用；自动识别不全时，手动把三个值分别填进对应的输入框。

---

## 三、完整使用流程

> 首次约 5 分钟，之后每学期只重复「提取 → 粘贴 → 导入」三步。
>
> 👉 如果你学校教务有「**导出课表为 Excel**」功能，可以跳过下面的第 2、第 3 步：直接在网页「2 · 提取课表」卡片底部的「📎 上传教务导出的课表表格」里选文件，一步拿到整学期课表（不需要装扩展、不需要点书签）。注意它**不含上下课时刻**，解析后要记得套用学校作息，详见第四节。

### 第 1 步 · 连接小爱

在网页顶部「**1 · 连接小爱**」卡片，把准备工作中点「**获取 userinfo**」得到的内容**整段粘贴**进大框（会自动识别三项凭据），点 **「连接」**。

- 成功：标题旁圆点变绿，提示「连接成功，共 N 个课表」；
- 失败：看红色提示，通常需要补齐某项凭据——把自动识别出的值核对一下即可。

### 第 2 步 · 存下提取书签（一次性）

在「**2 · 提取课表**」卡片点 **「📚 复制提取书签，粘贴到书签栏」**，然后：

1. 按 **Ctrl+Shift+B** 显示浏览器书签栏；
2. 书签栏空白处右键 → **「添加网页」**，标题填「提取课表」，网址框里 **Ctrl+V 粘贴** → 保存。

> 只需做一次。书签每次点击都会从后端拉取最新版提取脚本，不用维护版本。
> 想改成按钮常驻的油猴方案，见下文第四节；注意油猴脚本**不会自动更新**。

### 第 3 步 · 打开教务课表并提取

1. 在卡片里填你学校的**教务地址**（如 `jwc.htu.edu.cn`），点 **「🔑 打开登录页」** 登录；
2. 进入 **「📖 个人课表」**，**在页面上的学期下拉框里选中要提取的学期** —— 学期就是从那个下拉框读的，这一步决定提取哪个学期；
3. 点一下书签栏的 **「提取课表」** 书签。注意它**不直接出结果**，而是把「📚 提取课表」按钮装到页面右下角（脚本异步加载，会有短暂延迟）；
4. 点右下角那个 **「📚 提取课表」** 按钮；
5. 脚本自动：识别学期代码 → 逐周扫描全学期 → 合并单/双周 → 生成标准 JSON 并复制到剪贴板；
6. 看到绿色浮窗 **「✅ 已复制 N 条课表」** 即成功。浮窗会写明「学期 X，来源 Y」，可据此核对学期是否读对。

> **点了书签没反应？** 按 F12 → 网络，看有没有一条 `api/jiaowu_extractor.js` 请求：没有 = 书签没执行（检查书签网址开头是否还有 `javascript:`）；有但失败 = 后端没开（重启 `启动导入器.bat` 或重新复制书签）；返回 200 但仍无按钮 = 教务页面 CSP 拦了外部脚本，这种学校改用油猴。

### 第 4 步 · 粘贴、预览、导入

回到导入器网页：

1. 在「**4 · 粘贴课程 JSON 并导入**」点 **「📋 从剪贴板粘贴」**（或在框内 Ctrl+V）；
2. 页面自动生成 **星期 × 节次的彩色课表网格** —— 请核对课程落在正确的星期/节次/周次；
3. 在「**3 · 选择目标课表**」选一个已有课表，或点 **「＋ 新建课表」**（如「2026 秋」）；
4. 按需勾选：
   - **导入前清空**：课表里有旧课时勾上，避免重复；
   - **同步课表设置**：把教务的上午/下午/晚上节数与总周数一并写入，时间轴更准；
5. 点 **「导入到小爱 →」**，确认弹窗，等待「✅ 导入成功：新增 N 门课程」。

### 第 5 步 · 在小爱里验证

打开小爱同学 App → 课程表，确认课程显示正确。新建的课表若不是当前使用的，在 App 里切换一下。之后即可使用「小爱同学，我下一节什么课」等语音查询和上课提醒。

---

## 四、三种提取方式怎么选

| 方式 | 适合谁 | 入口 |
|---|---|---|
| **🥇 浏览器书签（推荐）** | 不想装扩展；希望脚本永远是最新版 | 「复制提取书签」→ 存进书签栏 → 教务页点书签 → 点右下角按钮 |
| **🥈 油猴脚本（Tampermonkey）** | 长期用，希望按钮常驻、不依赖后端随时可访 | 「复制油猴脚本内容」→ 装进扩展 → 教务页右下角按钮 |
| **🥉 上传教务导出表格** | 教务有「导出课表为 Excel」功能；不想装扩展也不想碰接口 | 「📎 选择教务导出的表格文件」→ 选 .xls/.xlsx → 自动预览 |
| **🤖 智能提取（兜底）** | 上面都用不了，或只有零星几门课 | 页面折叠区「智能提取」，粘贴接口 JSON / 页面文字 / 手打 |

**📎 表格通道的一个已知限制**：教务导出的表格里只有「第一大节 / 第二大节」这种**节次号**，**没有每节课的上下课时刻**（那个只能从接口的 `qssj/jssj` 或官方作息表拿）。所以走这条路解析完，务必在「课表设置」里点 **☀ 夏令时 / ❄ 冬令时**（识别到河南师大时会自动选中）或手填时间表，否则导入小爱后时间会不准。优点是它不依赖任何接口字段，因此不会出现星期偏移、节次认错、学期码取错这类问题，而且一次就是整学期。

**两种通道的实际差别**（不是“完全相同”）：

| | 🥇 书签 | 🥈 油猴 |
|---|---|---|
| 安装成本 | 零（存一个书签） | 要装 Tampermonkey 扩展 |
| 脚本版本 | **每次点击现拉，永远最新** | 装完即固化，**不会自动更新** |
| 是否依赖后端在线 | **是**，后端关了点了没反应 | 否，脚本存在本地 |
| 页面注入范围 | 只注入顶层框架 | 注入所有同源 iframe（能嗅探内层框架发出的请求） |
| 适用兜底 | 学期码只藏在 POST 载荷、又无学期下拉框的学校 | 同上这类难搞学校优先用油猴 |

**智能提取的三种输入**（点「🤖 智能提取」自动识别）：
1. 教务接口原始 JSON —— F12 → 网络 → `getCalendarWeekDatas` → 响应整段复制；
2. 课表页面文字 —— Ctrl+A 全选复制粘贴；
3. 手打一行一门课 —— 例：`周一 第1-2节 高等数学 张三 教1-101 1-16周(单)`。

---

## 五、支持的课表格式

### 手动 / 智能提取的标准课程格式

```json
[{ "name":"高等数学", "teacher":"张三", "position":"A101", "day":1, "sections":"1,2", "weeks":"1-16周(单)" }]
```

- `day`：1–7 = 周一至周日（也接受"周一""星期一"）
- `sections`：逗号分隔或范围，如 `1,2`、`3-5`、"第7-8节"
- `weeks`：`1-16周`、`1-16周(单)`、`2,4,6周`、`1至16` 等都认
- 字段名宽松兼容：`courseName`／`课程名称`／`老师`／`教室`／`星期`／`节次` 等别名都能识别

### 乘方教务原始数据

油猴脚本和智能提取也直接吃教务接口返回的原始行（`kcmc`、`qsxq`/`xq`、`ps`/`pe`、`jcdm`、`zc` 等字段），自动做星期/节次/周次转换。

---

## 六、常见问题

**Q：提取报错「学期 XXXX 下未查到课程」？**
A：学期代码猜错了。在教务页面**右键**「📚 提取课表」按钮 → 手动输入正确学期代码（如 `202601`）。学期代码可打开个人课表按 F12 → 网络，找 `getCalendarWeekDatas` 请求，看载荷里的 `xnxqdm` 值。

**Q：右下角出现多个「提取课表」按钮重叠？**
A：之前装过多个脚本副本。去 Tampermonkey 管理面板删掉多余条目，只留一个；或确认用的是最新版脚本（新版只在最外层显示一个按钮）。

**Q：课程全挤在某一天 / 星期不对？**
A：多为旧版脚本。重新「复制油猴脚本内容」→ 覆盖粘贴旧脚本 → 保存 → 教务页 F5。新版已修复河南师大的星期编码。

**Q：连接小爱提示凭据不完整？**
A：没点到「获取 userinfo」、或复制的内容不完整；也可能是 App 版本字段名不同。回到手机按「二、获取小爱课程表凭据」那一节：课程表设置页 → 拉到底 → 空白处连点五下 → 点「获取 userinfo」 → 整段粘贴。仍不行就手动把 appId / serviceToken / deviceId 分别填进三个小输入框再连接。

**Q：导入后小爱里时间不对 / 节数不对？**
A：勾选「同步课表设置」重新导入；或在预览下方的「课表设置」面板手动填上午/下午/晚上节数与总周数。

**Q：我学校不是河南师大，能用吗？**
A：只要是乘方教务就能试。若提取到的星期/节次/时间有偏差，把报错提示（会显示它读到的字段名和学期码）反馈出来即可快速适配。

**Q：隐私安全吗？**
A：教务登录凭证（Cookie）全程只在你浏览器里，提取脚本与表格解析都在你本机完成，不经过任何第三方；小爱凭据（appId / serviceToken / deviceId）存在**你本机浏览器的 localStorage**，以便下次自动回填——它只会被发往小米的课表服务器，我们的后端不存副本（服务端只在内存里保留会话，不落盘）。

> ⚠ 请知道：凭据**不会**因为关闭网页而消失。在机房、图书馆等公用电脑上使用时，用完请清除该站点的浏览器数据（目前页面内还没有「退出并清除凭据」按钮）。

---

## 七、本地运行与部署（进阶）

### 环境

- Node.js 18+（本机已装 v22 验证）

### 本地一键启动

双击项目根目录 **`启动导入器.bat`**：自动检测端口占用、启动服务、弹出浏览器 `http://localhost:8787`，关窗口即停止。
命令行等价：`node scripts/dev_server.mjs [端口]`（默认 8787）。

### 改代码后重新构建

提取脚本源头在 `tools/cf_extractor.js`。改完必须重新注入构建：

```bash
node scripts/build_functions.mjs
```

它会：① 把最新脚本内联进 Cloudflare 后端；② 生成独立油猴脚本 `tools/cf_kebiao_extractor.user.js`。

### 部署到 Cloudflare Pages（本项目线上环境）

**线上地址：<https://xiaoai-kebiao-zx.pages.dev>** —— 打开即用，不需要装 Node、不需要下载任何东西。

部署方式是 **Direct Upload + GitHub Actions**：推送到 `main` 分支时，`.github/workflows/deploy.yml` 在 CI 里跑 `wrangler pages deploy`，把仓库内容（含 `functions/` 后端）上传到 Pages，约 30 秒生效。

> 为什么不用 Pages 的「Git 集成」：`wrangler` CLI 建不了 Git 集成型项目，而官方明确 Git 集成项目之后**不能再改回 Direct Upload**，所以选了这条完全可脚本化、也不依赖控制台入口的路。

手工部署（与 CI 等价）：

```bash
npx wrangler login
npx wrangler pages project create <项目名> --production-branch main
npx wrangler pages deploy . --project-name <项目名>   # 在本仓库根目录执行
```

走 CI 需在 GitHub 仓库 Settings → Secrets and variables → Actions 里配两个 **Secret**：`CLOUDFLARE_API_TOKEN`（权限只需 Account / Cloudflare Pages / Edit）和 `CLOUDFLARE_ACCOUNT_ID`。两者都建议放 Secret 而非 Variable——公开仓库的 Actions Variables 是明文可读的。

可选环境变量：`AI_API_KEY`（启用服务端 AI 兜底解析）、`AI_BASE_URL`/`AI_MODEL`、KV 绑定 `COUNTER_KV`（持久化访问计数）。

### ⚠️ 不要拿 Cloudflare Worker 做对外分享的后端（国内访问不了）

`worker/` 目录保留了把同一套后端部署成 Worker 的能力，但**实测 `*.workers.dev` 在中国大陆被 DNS 污染、无法访问**（解析结果指向与本服务无关的地址），而 Pages 用的 `*.pages.dev` 可正常访问。

所以「GitHub Pages 放前端 + Worker 放后端」这套组合，在国内会得到一个**页面能打开、但所有 `/api` 请求全部失败**的部署，典型表现就是「点连接没反应」。除非你已经给 Worker 绑了自己的域名，否则请直接使用上面的 Cloudflare Pages 单站方案。

仅当你确有自定义域名时，才按下面步骤用 Worker：

1. `npx wrangler deploy -c worker/wrangler.toml`，然后**在 Worker 设置里绑定你自己的域名**（不要拿 `*.workers.dev` 对外分享）；
2. 前端访问时带参数 `?api=https://你的域名`（会记住到 localStorage）；或把该地址预置进构建；
3. 页面顶部出现「🔗 后端 API」横幅即生效，点「改用同源」可切回。

### 测试

```bash
node scripts/test_backend.mjs      # 后端路由
node scripts/test_frontend.mjs     # 前端解析逻辑
node scripts/test_extractor.mjs    # 提取脚本核心逻辑
```

---

## 八、技术栈与项目结构

- **前端**：原生 HTML/CSS/JS，**零构建、无框架**（`index.html`）；唯一第三方库是本地引入的 SheetJS（`vendor/xlsx.full.min.js`，约 880 KB，仅用于解析教务导出的 Excel），不走 CDN，因此仍可离线使用
- **后端**：Cloudflare Pages Function，一体化 `/api/*` + 小爱客户端（`functions/api/[[path]].js`）
- **提取脚本**：书签 / 油猴，直接调用乘方教务 JSON 接口（`tools/cf_extractor.js`）
- **小爱协议**：`https://i.ai.mi.com/course-multi-auth/*`

```
├── index.html                         # 导入器主页面
├── vendor/xlsx.full.min.js            # 唯一第三方库：解析教务导出的 .xls/.xlsx（本地引入，不走 CDN）
├── 启动导入器.bat                      # 一键本地启动
├── functions/api/[[path]].js          # 后端路由 + 小爱客户端
├── worker/                            # 把同一套后端部署成 Cloudflare Worker
├── tools/cf_extractor.js              # 提取脚本源（改这里）
├── tools/cf_kebiao_extractor.user.js  # 构建产物：独立油猴脚本
├── scripts/                           # build / dev_server / 三套测试
├── .github/workflows/deploy.yml       # push 即自动部署到 Cloudflare Pages
└── reference/                         # 调研资料（原项目、乘方API文档、样例）
```

---

## 九、能不能帮你做你学校的适配

本项目是按**教务系统厂商**适配的，不是按学校：只要你的学校用乘方科技教务系统，提取端大概率已经能直接工作。目前完整实测过的是**河南师范大学**，其他乘方部署可能有个别字段命名不同。

**先花两分钟判断能不能直接用**：登录教务 → `F12` → 网络标签 → 刷新课表页，找名为 `getCalendarWeekDatas` 的请求。找到了就是同一套系统，继续往下；找不到而看到 `jsxsd` 之类的，是强智教务，请用上面致谢里的上游原版工具。

**然后按顺序试这三个入口**（都不需要改代码）：

1. 教务若有「导出课表为 Excel」→ 用页面底部的 **📎 上传教务导出的课表表格**，最省事，而它不读任何接口字段，不会出现星期偏移、学期码取错这类问题（代价：表格里没有上下课时刻，要手动套用学校作息）
2. **书签提取** → 教务页点书签 → 点右下角「📚 提取课表」
3. **🤖 智能提取** → 把 `getCalendarWeekDatas` 的**响应原文**整段粘进去

**三个都不行？提 issue 时带上这些信息，基本一次就能定位**（缺了就只能来回猜）：

```text
1. 教务域名（如 jwc.xxx.edu.cn）+ 上面能不能找到 getCalendarWeekDatas
2. 该请求的【载荷】原文（看得到 xnxqdm / zc / d1 / d2 这些参数）
3. 该请求【响应】里的 1~2 条完整记录——请先把姓名、学号等个人信息的值删掉
4. 一门你确定在上过的课的真实情况：星期几、第几节、哪几周、几点上课
5. 你们学校作息：一天几节、一节课多少分钟、课间多少分钟
6. 报错原文——提取失败时，页面会把「它实际读到的字段名」和一条示例记录打到 F12 控制台，那段最有用
```

**想自己改代码**：[`适配指南.md`](适配指南.md) 是新学校接入的完整手册——六个必须回答的问题（课程名字段 / 星期编码 / 节次形式 / 周次来源 / 上下课时间 / 学期代码）、每个问题怎么自己查、要改哪几行、以及河南师大适配过程中踩过的九个坑。

> ⚠ 自己动手时最容易漏的一条：字段解析在 `tools/cf_extractor.js` 和 `index.html` 里**各写了一份**（分别服务「脚本提取」和「粘贴原始 JSON」两条通道），**必须同时改两处**。只改一份会得到“脚本能提取、粘贴解析不出课程”这种看起来随机的故障。

## 十、致谢

- 原项目（强智教务版全栈）：[tangjiafu430-bot/xiaoai-kebiao-importer](https://github.com/tangjiafu430-bot/xiaoai-kebiao-importer)
- 乘方教务接口逆向文档：[xyj19/chengfang-edu-api-docs](https://github.com/xyj19/chengfang-edu-api-docs)
- 乘方教务 Python SDK 参考：[RS-Nocsi/wyu-jwxt](https://github.com/RS-Nocsi/wyu-jwxt)

## 许可证

MIT License。

> **免责声明**：仅供学习交流。教务系统接口为厂商私有协议、可能随版本变化；请遵守所在学校的使用规范，导入前建议先「导出 JSON」备份。
