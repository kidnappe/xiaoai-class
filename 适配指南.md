# 适配指南 · 把你的学校接进「小爱课表导入器」

> 面向：用**乘方科技教务系统**、但本工具尚未适配其学校的高校同学。
> 本指南把河南师大（`jwc.htu.edu.cn`）适配过程中踩出来的全部经验，整理成可照着执行的步骤。
> 前提：你不需要会写后端，也不需要懂小爱协议——**协议侧一行都不用改**，要改的只有「怎么把你们学校教务返回的数据读成课程」。

---

## 〇、先说结论：适配一所新学校的工作量在哪

| 部分 | 要不要动 | 说明 |
|---|---|---|
| 小爱 API（认证、读写、切表、清空、节次设置） | ❌ 不用动 | 全学校通用，已验证 |
| 教务**提取**（字段名、编码、页面路径、学期码） | ✅ 全部工作在这 | 见第三节六类事实 |
| 学校官方作息预设（可选） | ⚠ 可不加 | 只有自动推导不准时才需要 |
| 预览 / 编辑 / 导入 / 冲突校验 UI | ❌ 不用动 | 通用 |

也就是说：**适配 = 回答 6 个问题 + 改 2 个文件里的字段候选**。河南师大当时真正的难点不在代码量，而在于没人告诉你要回答哪 6 个问题，全靠抓包一层层试。下面就是那 6 个问题。

---

## 一、十分钟判断你的学校是不是同一套系统

不用装任何东西，先做这一步。不满足就别继续，省下几小时。

1. 登录教务系统，看地址栏：通常是 `jwc.你的学校.edu.cn` 或 `jxgl.你的学校.edu.cn`，登录后路径里带 `/new/`。
2. 打开「个人课表」页面，按 `F12` → **网络（Network）** 标签 → 刷新页面。
3. 在请求列表里找名字叫 **`getCalendarWeekDatas`** 的 XHR 请求。

- **找到了** → 你的学校是乘方系统，继续第二节。
- **找不到，但看到 `jsxsd`、`xsxx`、`xskb_list` 之类** → 是**强智教务**，本工具不适用，请用原版：[tangjiafu430-bot/xiaoai-kebiao-importer](https://github.com/tangjiafu430-bot/xiaoai-kebiao-importer)。
- **两者都不是** → 可能是正方、URP 或其它厂商，本工具的提取端不适配，需要另写。

---

## 二、必须采集的原始样本（做这一步再动手改代码）

打开那个 `getCalendarWeekDatas` 请求，把两样东西各存成文件，别丢：

1. **载荷（Payload / 表单数据）** —— 一整份，重点看有没有 `xnxqdm`、`zc`、`d1`、`d2`、`szxqdm`、`yxdm`。
2. **响应（Response）** —— 一整份 JSON。**这是最关键的原始资料**，第三节六个问题全部从它读答案。

> 响应里含你的个人信息（学号、姓名、课表），只在你要别人帮忙时发给信任的人；不要贴到公开 Issue 里，把 `xsxm`（学生姓名）、`xh`（学号）一类的值删掉再发。

顺手再记两个**只有你自己知道的事实**，后面用来校验编码：

- 某一门你确定在上过的课，它实际是**星期几**、**第几节**、**哪几周**。
- 你们学校**一节课多少分钟、课间多少分钟、一天几节、上午/下午/晚上各几节**。

---

## 三、六个问题（这就是「适配」的全部内容）

对着第二节的响应样本，逐个回答。每个问题都给了「河南师大的答案」当参照。

### Q1 课程名在哪个字段？

在响应里找到一门课的那条记录（`data` 数组里的一个对象），看课程名是什么键。

- 河南师大：**`kcmc`**
- 常见变体：`kcName`、`courseName`

### Q2 星期在哪个字段，数字怎么编码？

**这是最容易出错的一项，错了的症状是"课程全部挤在某一天"或"整体偏移一天"。**

河南师大：字段是 **`qsxq`**（也有 `jsxq`），值是 **Java `DAY_OF_WEEK` 编码**——`2`=周一、`3`=周二 … `7`=周六、**`1`=周日**。所以要统一 `-1`（`1` 特判成 `7`）才变成「1=周一 … 7=周日」。

**怎么自己验证（别猜，一定要做）**：挑一门你确切知道是周几的课，看它在响应里的字段值：

| 你上课实际是 | 字段值若是 | 说明编码是 | 处理 |
|---|---|---|---|
| 周一 | `2` | Java DAY_OF_WEEK | 需要 `-1`，周日 `1`→`7` |
| 周一 | `1` | 1=周一 直给 | **不要**偏移 |
| 周日 | `1` | Java | 特判成 7 |
| 周一 | `"星期一"` / `"周一"` | 中文文本 | **绝不偏移**，按字面映射 |

最后一行很重要：如果字段值有时是数字有时是中文，**中文那条路径不能做 -1**，否则"星期一"会变成"周日"。当前代码用 `pickDay()` 返回 `{ d, numeric }` 区分这两种情况，就是为这个。

### Q3 节次（第几节）在哪个字段，什么形式？

三种已见过的形式，你的学校可能是其中一种：

| 形式 | 例 | 河南师大 | 说明 |
|---|---|---|---|
| 起止两个数字字段 | `ps:"3"`, `pe:"4"` | ✅ 用这个 | 表示第 3–4 节 |
| 单个节次代码串 | `jcdm:"0304"` / `jcdm2:"09,10"` | 兼容 | 4 位连写 = 起止两节 |
| 节次名文本 | `"第3-4节"` | 兼容 | 靠正则抠数字 |

同时确认**你们一天最多几节**。河南师大是 10 节（上午 4 / 下午 4 / 晚上 2）。这个数字决定默认时间表要截到多长。

### Q4 周次在哪个字段，是整学期还是一周？

**这决定提取要发 1 次请求还是 19 次。**

- 河南师大：记录里的 **`zc` 就是整学期周次串**，形如 `"1-19"`、`"2-18(双)"`、`"1,5,9,13"` → 可以**一次请求拿全学期**（代码里的"快速路径"）。
- 若你的学校 `zc` 只有当前周（如 `"5"`），就必须**逐周扫描**，一周一次请求。当前代码已经会自动判断并在拿不到周次时退回调周扫描，你不用改。

单双周要确认写法：`(单)` / `(双)` / `单周` / `双周` 都可能出现，`parseWeekNums()` 已按「含"单"/"双"字」过滤，一般不用改。

### Q5 每节课的上下课时间从哪来？

**这是河南师大适配里最大的一个坑，别重犯。**

按可靠性从高到低，只用这三档：

1. **课程行自带的 `qssj` / `jssj`** —— 最可靠。这是该连堂块的真实起止时刻，比如第 1–2 节的课带 `qssj:"08:00"`、`jssj:"09:40"`。当前代码用 `buildSectionsFromTimes()` 以这些值为锚点反推整张节次表，只有单侧时按「45 分钟一课 + 10 分钟课间」补另一侧。**河南师大 10 节全部命中官方夏季表，就是靠这个。**
2. **学校官方作息文件 / 教务处通知** —— 当第 1 档缺失或不准时用。做成预设表内置进前端（见第五节）。
3. **兜底默认表 `DEFAULT_SECTIONS`** —— 13 节的通用表，几乎一定不准，只是保证不空白。

> ⚠️ **绝对不要用页面里的 `businessHours`。** 课表页面是 FullCalendar，`week.page` 内嵌了一段 `var businessHours`，看着像时间表，其实是**拖拽/吸附网格的背景块**——每段 75~125 分钟且首尾互相重叠。把它当节次表会产出"第 1 节 08:00–09:15"这种值。这个错误在本项目犯过一次并已整套删除，别把它请回来。

### Q6 学期代码怎么取？

学期代码（`xnxqdm`）形如 `202601` = 2026 学年第 1 学期（秋季），`202602` = 春季。

**麻烦在于它在页面上藏得深**，河南师大踩过两层：

- 第一层：它**不在顶层 URL 里**，只出现在 POST 载荷里，所以光看地址栏看不到。
- 第二层（更要命）：课表页是多层**同源 iframe**（`welcome.page → main.page → week.page`），学期是 `week.page` 里的一个 `<select name="xnxqdm" id="xnxqdm">`。**切换学期后，该 iframe 的 `src` 属性并不更新**，还停在初始学期；只有它的实时 `location.href` 和 select 的选中值才是新的。早期版本在父层先匹配到 `src` 就 return，导致永远提取初始学期。

现在的取值优先级（`getTermAndWeek`）：

```
右键手动钉死(cfjw_term_manual) > 页面 URL > 递归进同源 iframe 读 select 选中值
  > iframe 实时 location.href > 已加载资源记录 > 嗅探记忆(cfjw_term) > 按日期推算
```

**你要确认的是**：你们学校是不是也有一个学期下拉框、`name`/`id` 里含 `xnxqdm`。如果不是（比如叫别的名字，或者是按钮翻页而非下拉框），就需要给 `findTermInDom()` 增加一个匹配条件。

**最省事的验证方法**：装好提取脚本后点右下角按钮，成功/失败的浮窗会显示「**学期 X，来源 Y**」。你切换学期下拉框到目标学期再提取，这行的「学期」应当跟着变、来源应显示「页面学期下拉框」。不变就是这一项没适配。

---

## 四、改代码：具体改哪几行

### ⚠️ 先读这段：字段解析存在**两份独立实现**，必须同时改

这是移植时最容易漏的事，也是本指南最重要的一条。同一套「把教务记录变成课程」的逻辑在两个地方各写了一份，服务于两条不同的数据通道：

| 实现位置 | 服务的通道 | 谁在用 |
|---|---|---|
| `tools/cf_extractor.js` 的 `xferRow()` | 油猴脚本 / 书签提取 | 走脚本一键提取的人 |
| `index.html` 的 `cfRowsToCourses()`（约 1131 行起） | 网页「智能提取」粘贴原始 JSON | 不用脚本、直接 F12 复制响应粘贴的人 |

**只改一份的后果**：油猴提取正常，但粘贴原始 JSON 解析不出课程（或反之）——症状看起来像随机故障，很难想到是两处不同步。

而且这两份**当前已经存在偏差**（不是假设，是实测）：

```js
// tools/cf_extractor.js:366
var name = cleanText(r.kcmc || r.kcName || r.courseName || '');
// index.html:1196
var name = cleanText(r.kcmc || r.courseName || r.name || "");
```

提取器的候选链里有 `kcName`，前端没有；前端有 `name`，提取器没有。河南师大两条通道都正常，只是因为两边都命中了第一个 `kcmc`，掩盖了这个不一致。**你加新字段时，请两处都加，并顺手把两条链对齐。**

### 4.1 提取器 `tools/cf_extractor.js`（唯一编辑入口）

| 要改的东西 | 位置 | 怎么改 |
|---|---|---|
| 课程名候选 | `:366`、`:466`（两处，:466 是诊断计数用的，别漏） | 把你们学校的键加进 `||` 链 |
| 星期字段候选 | `:318` `var cands = [r.qsxq, r.xq, r.jsxq, r.day]` | 换成你们字段，如 `r.xqj` 加进去 |
| 星期编码 | `pickDay()` 返回后的转换（`pd.d === 1 ? 7 : pd.d - 1`） | **Q2 若是 1=周一 直给，就要去掉这个偏移**，否则全表错一天 |
| 节次字段 | `:333` `pickSections()`，先 `ps`/`pe` 再 `jcdm2`/`jcdm` | 加你们的字段分支 |
| 教师候选 | `:376` `teaxms \|\| teaxm \|\| jsxm` | 同上 |
| 教室候选 | `:375` `zdjxcdmc \|\| jxcdmc \|\| cdmc` | 同上 |
| 周次字段 | `xferRow()` 内 `parseWeekNums(r.zc)` | 若叫别的名字改掉 |
| 页面路径 | `:429` `week.page`，`:437` 回退 `main.page` | 你学校若路径不同（如 `/new/student/kb/...`），改这两行 |
| 接口路径与参数集 | `:447` `getCalendarWeekDatas` + `{szxqdm,xnxqdm,yxdm,zc,d1,d2}` | 照你抓到的**载荷原样**对齐，多参数就加，少参数别硬塞 |
| 学期码语义 | `termWindow()` / `termStart()` / `guessTerm()` | 若你们学期码格式不是 `YYYYSS`、或开学月不是 9 月/3 月，要改 |
| 噪声过滤词 | `SKIP_KEYWORDS` | 「预选阶段」「选课阶段」等，你们学校有别的提示文字就加进去 |
| 会话失效识别 | `sessionExpired()`：`login-form`、`统一身份认证`、`code:-401` | 你们 CAS 登录页特征不同就补 |
| 默认节次表 | `:106` `DEFAULT_SECTIONS`（13 节） | 只是兜底，一般不用动 |

### 4.2 前端 `index.html`

- `cfRowsToCourses()`（`:1131` 起）：星期候选 `:1146`、节次 `:1159`、名字 `:1196`、教师 `:1222`、教室 `:1223` —— **与 4.1 一一对应，同步改**。
- `DEFAULT_SECTIONS` `:1065`、`buildSectionsFromTimes` `:1091`：前端自己的一份，与提取器同源。若你改了推导规则（例如你们一节课是 50 分钟），两处都要改。
- `:1436` 判定"粘贴的是不是课程 JSON"的正则 `/"(name|courseName|kcmc)"\s*:/i`：如果你的学校课程字段不在里面，粘贴原始 JSON 会被当成非课表内容。

### 4.3 后端 `functions/api/[[path]].js`

**不要手改**里面 `const CF_EXTRACTOR_JS = ...` 那一整块——它是构建产物，会被下次构建覆盖。其它手写逻辑（路由、`normalizeSchedule`、`XiaoaiApi`）与学校无关，通常不用动。`normalizeSchedule` 已经在 `:238`/`:242` 透传 `host`，新增学校预设不需要改后端。

---

## 五、可选：把你们学校的官方作息做成一键套用

只有在**自动推导（Q5 第 1 档）不准**时才需要做。如果你们课表数据带了可靠的 `qssj`/`jssj`，跳过这节。

当前实现是**河南师大单例**，写死在前端三处：

- `index.html:625` `const HTU_SCHOOL = { name, summer:[[节,"起","止"],…], winter:[…] }`
- `index.html:634` `function isHtuHost(host){ return /(^|\.)htu\.edu\.cn$/i.test(…) }`
- `index.html:709` `<option value="htu">河南师范大学</option>`

外加 `detectSeason()`（`:645-646`）、`applyHtuTimes()`（`:652`）、面板自动选中（`:728`）。

**要加第二所学校**，照这个形状加三项（下拉选项 + 数据 + 域名判断）能跑，但会迅速变成一堆 `if (isXxxHost)`。**建议直接抽成表**，这是本文明确推荐的改法：

```js
// 学校作息预设：新增学校只加一项，不改逻辑
const SCHOOL_PRESETS = [
  {
    key: "htu",
    name: "河南师范大学",
    match: /(^|\.)htu\.edu\.cn$/i,
    summer: { morningNum: 4, afternoonNum: 4, nightNum: 2,
              sections: [ ["08:00","08:45"], /* …共 10 节… */ ] },
    winter: { /* 同形状 */ },
  },
  // 你的学校加在这里
];
```

然后 `isHtuHost()` 换成 `SCHOOL_PRESETS.find(p => p.match.test(host))`，下拉用 `SCHOOL_PRESETS.map(...)` 生成，`applyHtuTimes`/`detectSeason` 接收 preset 参数而不是读单例。改动量约二三十行，现有测试 `test_frontend.mjs:171-174` 可以直接照抄成两条用例。

> 作息数据请来自**学校官方文件或教务处通知**，别凭记忆填。河南师大那张表是用户拿两张官方截图逐节核对录进来的（含夏/冬两套，冬季第 5–10 节整体提前 30 分钟）。

---

## 六、构建、验证、以及「怎么确认我改对了」

### 6.1 改了 `tools/cf_extractor.js` 之后必须做这两步

```bash
node scripts/build_functions.mjs     # ① 注入后端 + 生成独立油猴脚本
# ② 重启本地服务：函数在启动时把脚本读进内存，不重启页面拿到的还是旧脚本
```

只改 `index.html` 不用 build，刷新即生效（dev_server 已加 no-cache）。

> 若你连最终用户脚本的自动更新都要管，可以给油猴头加 `@updateURL`/`@downloadURL` 指向你的部署地址（当前未做）。

### 6.2 跑测试

```bash
node scripts/test_extractor.mjs      # 提取器核心
node scripts/test_frontend.mjs       # 前端解析
node scripts/test_backend.mjs        # 后端路由
```

当前是 **42 / 37 / 15 全绿**。你改完应当仍然是全绿——**这三套测试是回归保护，你的改动不该让它变红**。变红说明你动了通用逻辑，回头用「只在候选链里加字段」的方式改，别删已有分支。

### 6.3 加你自己学校的用例（强烈建议，这是交付的一部分）

照 `scripts/test_extractor.mjs:73-81` 的形状加，那里是河南师大样本：

```js
const mine = { kcmc: '高等数学', qsxq: '2', ps: '1', pe: '2', zc: '1-16',
               jxcdmc: '3教101', teaxms: '王老师', qssj: '08:00', jssj: '09:40' };
const c = t.xferRow(mine, 5);
check('xferRow <你的学校> 字段', c && c.day === 1 && c.sections === '1,2', JSON.stringify(c));
```

字段值请**脱敏**：课名用「高等数学」这类通用名，教师用「王老师」，教室用「3教101」。别把真实课程和姓名提交进公开仓库。

再加一条节次表推导用例（对照 `test_extractor.mjs:115-127`）：把你们几个连堂块的 `qssj`/`jssj` 填进去，断言推导结果等于官方作息表。**这条通过，才算真适配。**

### 6.4 真机验收清单

按顺序全过才算成：

1. 教务页点「📚 提取课表」，浮窗显示「学期 X，来源 Y」，**X 是你切换到的那个学期**（Q6）。
2. 提取到的课程条数和你在教务网页上看到的课程数一致（不多不少）。
3. 导入器预览网格里，随机抽 3 门课核对：**星期对、节次对、周次对（尤其单双周）**（Q2/Q3/Q4）。
4. 课表设置面板的时间表，抽 3 节对**你们学校真实上下课时间**（Q5）。
5. 导入 → 在**小爱 App 里切到那个课表**看是否显示（小爱读写绑定「当前使用课表」，工具会自动切，但 App 端仍要你自己确认看的是那张表）。
6. 单双周课、跨周课程（如 1-16 周单周）显示正确。

---

## 七、小爱侧：这些不用碰，但要知道

适配学校时不需要理解这些，但排错时会碰到：

- **`weeks` 必须是展开的数字 CSV**（`1,3,5,7`）发给小爱；展示用的压缩串（`1-15周(单)`）是 `weeksText`。预览里改周次要走 `expandWeekInput()` 转回数字串，否则写进坏数据。
- **同「星期+节次+周次」重叠会被拒**：小爱返回 `code:601 course info has overlap`。单双周错开不算重叠。症状是「导入失败/没提示」，解法是勾「导入前清空」或换个空课表。前端已在解析和导入前做 `findConflicts()` 校验。
- **读写绑定「当前使用课表」**：导到非当前表时写入会落到当前表（表现为接口回 `code:0` 且条数正确，但目标表回读 0）。后端 `/api/import` 已通过 `table_switch` 自动切表。
- **凭据账号要和手机小爱 App 同一账号、同一设备**，否则写进去了但你在 App 里看不到。

---

## 八、河南师大适配踩过的坑（别重复）

1. **星期编码不是 1=周一**：河南师大是 Java `DAY_OF_WEEK`（2=周一…1=周日）。不偏移就整体错一天；给中文星期名做偏移会把「星期一」变成周日。
2. **`businessHours` 不是课时表**：它是 FullCalendar 拖拽背景块（75~125 分钟且首尾重叠）。用它导致"第 1 节 08:00–09:15"。已整套删除。
3. **学期码只在 POST 载荷里**，顶层 URL 看不到 → 需要 XHR/fetch 被动嗅探 + 右键手动钉死双兜底。
4. **iframe 的 `src` 属性切学期后不更新** → 必须递归进同源 iframe 读 `<select>` 选中值，优先级：递归子文档 > 实时 href > 资源记录 > `src` 属性（最后兜底）。早期在父层匹配 `src` 就 return，永远提取初始学期。
5. **门户是多同源 iframe**（`welcome.page` → `main.page` → `week.page`，地址栏始终停在 `welcome.page` 属正常）→ 油猴注入所有 frame，**按钮 UI 必须 `window.top === window.self` 守卫**，否则右下角叠出多个按钮；学期嗅探器则要留在所有 frame 才能捕获请求。
6. **`zc` 行内是整学期串** → 可以一次请求成表；不知道这一点就会白跑 19 次逐周扫描。
7. **节次两套字段**：`ps`/`pe`（起止数字）与 `jcdm`/`jcdm2`（`"0304"`、`"09,10"`），同一部署可能混用，`parseJcdm()` 两种都要吃。
8. **接口"有数据但一条都解析不出"要报错而不是静默失败** → `diag()` 会把读到的真实字段名列表和示例记录打印到 F12，这是适配别的学校时最有用的诊断。
9. **改源码别用 PowerShell `Set-Content` 回写**：Windows PowerShell 5.1 默认按系统码页写，会把含中文的 UTF-8 源码写坏成乱码。用编辑工具改。

---

## 九、请别人帮忙时的信息格式

带着下面这些去问，一次就能定位；缺了就只能来回猜。

```text
1. 教务域名（如 jwc.xxx.edu.cn）+ 是否乘方系统（Q1 判据）
2. getCalendarWeekDatas 的【载荷】原文
3. getCalendarWeekDatas 的【响应】里 1~2 条完整记录（删掉姓名学号等个人信息）
4. 六个问题的你的答案：课程名字段 / 星期字段+编码 / 节次字段形式 /
   周次字段+是否整学期 / 时间字段有没有 qssj-jssj / 学期码怎么取
5. 一门已知课程的真实情况：星期几、第几节、哪几周、几点上课
6. 学校作息：一天几节、一节课多少分钟、课间多少分钟
7. 实际症状 + F12 控制台里 [小爱课表提取器] 的报错原文（会带真实字段名）
```

---

## 十、一句话总结

适配新学校 = 回答第三节那 6 个问题，然后**在两个文件里同步改字段候选**（4.1 + 4.2，别只改一份），最后用 6.3 的节次推导用例和 6.4 的六步真机清单验收。协议、导入、UI 都不用碰。
